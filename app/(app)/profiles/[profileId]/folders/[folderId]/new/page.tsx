interface NewFilePageProps {
  params: { folderId: string; profileId: string };
}

const NewFilePage: React.FC<NewFilePageProps> = () => {
  // const { folderId, profileId } = params;

  return (
    // <FolderFileContainer
    //   profileId={profileId}
    //   folderId={folderId}
    //   action="new"
    // />
    <>lalla</>
  );
};

export default NewFilePage;
